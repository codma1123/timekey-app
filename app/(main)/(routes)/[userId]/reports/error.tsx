"use client";

type ReportsErrorProps = {
  error: unknown;
  reset: () => void;
};

const ReportsError = ({ error, reset }: ReportsErrorProps) => {
  return <div>머징?</div>;
};

export default ReportsError;
