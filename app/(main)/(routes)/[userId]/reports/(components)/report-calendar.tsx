import { ReportSummary } from "@/types/report";

import { Calendar } from "@/components/ui/calendar";

const ReportCalendar = ({ reports }: { reports: ReportSummary[] }) => {
  return <></>;
};

export default ReportCalendar;
